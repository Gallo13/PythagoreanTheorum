class PythagoreanTheorumClass
{
	double a, b, c;
	double result;

	if(a == ? || a = "")
		sum = Math.sqrt(Math.pow(c,2) - Math.pow(b,2));
	else if(b == ? || b = "")
		sum = Math.sqrt(Math.pow(c,2) - Math.pow(a,2));
	else if(c == ? || c = "")
		result = Math.sqrt(Math.pow(a,2) + Math.pow(b,2));

	PythagoreanTheorum myTheorum = new PythagoreanTheorum

	result = getResult();
}