import java.swing.*;
import java.awt.*;
import java.awt;

public class PythagoreanTheorumApplet
{
	public JApplet PythagoreanTheorumApplet implements ActionListener
	{
		// Data Fields
		JTextField A = new JTextField(10);
		JTextField B = new JTextField(10);
		JTextField C = new JTextField(10);
		JButton calc = new JButton("Calculate");
		JButton clear = new JButton("Clear");
		JTextField result = new JTextField(10);

		JLabel ALab = new JLabel("Enter the opposite (a): ");
		JLabel BLab = new JLabel("Enter the adjacent (b): ");
		JLabel CLab = new JLabel("Enter the hypotenuse (c): ");
		JLabel resultLab = new JLabel("The sum is: ");

		JPanel APane = new JPanel();
		JPanel BPane = new JPanel();
		JPanel CPane = new JPanel();
		JPanel buttonPane = new JPanel();
		JPanel resultPane new JPanel();

		getContentPane()

	// Contructor
	public PythagoreanTheorum()
	{
		
	}
	
	{
		if( ("Calculate"))
		
		else if( ("Clear"))
		JTextField A = "";
		JTextField B = "";
		JTextField C = "";
		JTextField result = "";
	}
	
}

		

		